export default {
    plugins: {
        // With @tailwindcss/vite, we don't need tailwindcss or autoprefixer here 
        // as the Vite plugin handles them.
        // This file exists primarily to prevent picking up postcss.config.js 
        // from parent directories.
    },
}
