const contabilidadService = require('./services/contabilidadService'); async function run() { try { const req = { input: () => {}, query: async (q) => { const { getPool } = require('./config/db'); const pool = await getPool(); const request = pool.request(); request.input('CueIdCuenta', 2521); request.input('FechaDesde', '2026-05-09'); request.input('FechaHasta', '2026-06-09'); return request.query(q); } }; const result = await contabilidadService.getMovimientos(2521, '2026-05-09', '2026-06-09'); console.log('getMovimientos returned:', result); } catch(e) { console.error(e); } finally { process.exit(0); } } run();
