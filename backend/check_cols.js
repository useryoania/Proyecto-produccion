const { getPool } = require('./config/db'); getPool().then(async pool => { try { const result = await pool.request().query(` SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'DocumentosContables' AND (COLUMN_NAME LIKE '%Cotiz%' OR COLUMN_NAME LIKE '%Dolar%' OR COLUMN_NAME LIKE '%Moneda%') `); console.table(result.recordset); } catch(e) { console.error('SQL Error', e); } process.exit(); });
