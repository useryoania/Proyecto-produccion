const { getPool, sql } = require('./backend/config/db'); getPool().then(async pool => { try { const cli = await pool.request().query(\SELECT * FROM dbo.Clientes WHERE CliNombre LIKE '%CamiEspi22%'\); console.log('Cliente:', cli.recordset); if(cli.recordset.length > 0){ const planes = await pool.request().input('c', cli.recordset[0].CliIdCliente).query(\SELECT * FROM dbo.PlanesMetros WHERE CliIdCliente = @c\); console.log('Planes:', planes.recordset); } } catch(e){console.error(e)} process.exit(0); });
